using System;
using System.Collections.Generic;
using System.Text;

namespace KavoshFrameWorkCore
{
    public enum FormStatus
    {
        Pending = 0,
        Verified = 1,
        Rejected = 2
    }
    public enum PerformanceEvaluationFormItemType
    {
        CompanyOnly = 0,
        HoldingOnly = 1,
        Both = 2
    }
}
